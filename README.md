# Deep-Learning-chapter-2

## 编程练习1
### 使用numpy实现前馈神经网络
说明：由于官方提供的练习中，代码似乎都已经填好了，我们直接从零开始自己实现。

由于我相对熟悉pytorch框架，因此在 `chapter4_ex1` 文件夹中实现了 `纯numpy` 和 `numpy + pytorch` 两个版本，为加快训练速度，`pytorch` 使用了GPU版本。
其中，`mnist_numpy.ipynb` 中是 `纯numpy` 版本的实现，可以达到 94.1% 的准确率；`mnist_pytorch.ipynb` 中是 `numpy + pytorch` 版本的实现，可以达到 97.8% 的准确率。

## 编程练习2
### 使用基于ReLU的神经网络来拟合一个有界闭集函数

